// AnimaLab — day01_variables
fn main() {
    /*let monto = "1000";
    monto = 2000;
    println!("{}", monto);

    let monto = monto + 500;
    let monto = monto * 2;
    println!("{}", monto);

    let monto = monto.parse::<i32>().unwrap();
    println!("{}", monto + 500);*/

    let _estado = "pendiente";
    let estado = 1;
    println!("{}", estado);
}
